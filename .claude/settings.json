{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "timeout": 30,
            "command": "cd /c/Users/marit/Documents/gravida-booking 2>/dev/null || exit 0; git status --porcelain --untracked-files=no 2>/dev/null | grep -q . || exit 0; git add -u 2>/dev/null; git commit -m 'Auto-commit: changes from Claude session' 2>/dev/null; git push 2>/dev/null; exit 0"
          }
        ]
      }
    ]
  }
}
